import 'babel-polyfill';
import * as RxDB from 'rxdb';
/*
RxDB.create({
  name: 'heroesdb',
  adapter: 'websql',
  password: 'myPassword',
  multiInstance: true
}).then((db) => {
  return db.collection({
    name: 'heroes',
    schema: 'mySchema'  
  })
}).then((collection) => {
  collection.insert({name: 'Bob'})
})
*/
